# Gói dependency GỌN cho bản public dashboard (chế độ Heuristic Scorer).
# KHÔNG gồm torch / torch-geometric / qiskit* / dwave-neal — nhánh heuristic
# của dashboard không cần chúng, và các nền miễn phí (Streamlit Cloud) sẽ
# fail nếu phải build cả stack nặng đó.
streamlit>=1.40
pandas>=2.2
numpy>=1.26,<2
pyarrow>=15
plotly>=5.20
networkx>=3.2
pulp>=2.8
matplotlib>=3.7          # pandas Styler.background_gradient (tô màu bảng cảnh báo ở Khối 1)
